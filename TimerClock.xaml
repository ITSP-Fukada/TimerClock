<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Timer Clock" 
        SizeToContent="WidthAndHeight"
        MinWidth="380" MinHeight="320"
        Topmost="True"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Name="TimeDisplay"
                       Grid.Column="0"
                       FontSize="44"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Text="HH:mm:ss"/>
            <TextBlock Name="RemainingTimeDisplay"
                       Grid.Column="1"
                       FontSize="20"
                       FontWeight="SemiBold"
                       VerticalAlignment="Bottom"
                       HorizontalAlignment="Right"
                       Text=""
                       Margin="10,0,0,5"/>
        </Grid>

        <CheckBox Name="AlwaysOnTopCheckbox"
                  Grid.Row="1"
                  Content="常に手前に表示"
                  HorizontalAlignment="Left"
                  Margin="0,10,0,0"
                  IsChecked="True"/>

        <StackPanel Grid.Row="2" Margin="0,10,0,0">
            <RadioButton Name="AlarmRadio" Content="アラーム機能" IsChecked="True" GroupName="ModeSelection" Margin="0,5,0,0"/>
            <RadioButton Name="PomodoroRadio" Content="ポモドーロ機能" GroupName="ModeSelection" Margin="0,5,0,0"/>

            <StackPanel Name="AlarmControls" Orientation="Vertical" Margin="0,10,0,0" Visibility="Visible">
                <TextBlock Text="アラーム時刻設定:" Margin="0,0,0,5"/>
                <StackPanel Orientation="Horizontal">
                    <ComboBox Name="AlarmHourInput" Width="55" Margin="0,0,2,0"/>
                    <TextBlock Text="時" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox Name="AlarmMinuteInput" Width="55" Margin="0,0,2,0"/>
                    <TextBlock Text="分" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button Name="SetAlarmButton" Content="設定" Width="60"/>
                    <Button Name="CancelAlarmButton" Content="消去" Width="60" Margin="5,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Name="QuickPlus5" Content="+5分" Width="50" Margin="0,0,5,0"/>
                    <Button Name="QuickPlus10" Content="+10分" Width="50" Margin="0,0,5,0"/>
                    <Button Name="QuickPlus30" Content="+30分" Width="50" Margin="0,0,5,0"/>
                    <Button Name="QuickPlus60" Content="+1時間" Width="60"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Name="PomodoroControls" Orientation="Vertical" Margin="0,10,0,0" Visibility="Collapsed">
                <TextBlock Name="PomodoroStatusText" Text="ポモドーロ: 準備完了" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock Name="PomodoroTimerDisplay" Text="00:00" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Name="StartPomodoroButton" Content="開始" Width="80" Margin="0,0,5,0"/>
                    <Button Name="PausePomodoroButton" Content="一時停止" Width="80" Margin="0,0,5,0"/>
                    <Button Name="ResetPomodoroButton" Content="リセット" Width="80"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <StatusBar Grid.Row="3" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Name="StatusText" Text="準備完了"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>